tls:
  certificates:
    - certFile: /certs/fullchain.cert
      keyFile: /certs/privkey.key

  stores:
    default:
      defaultCertificate:
        certFile: /certs/fullchain.cert
        keyFile: /certs/privkey.key
